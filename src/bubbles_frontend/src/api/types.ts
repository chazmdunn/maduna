export type TweetsData = {
  value: string;
  created_at: string;
};

export type Tags = {
  name: string;
  popularity: number;
};
export type TrendsData = {
  name: string;
  scale: number;
  color: string;
};
